$(document).on('ready', function () {

    //$(".slick-slider-testimonial").
    $('.slick-slider-testimonial').slick({
      slidesToShow: 1,
      centerMode: true,
      centerPadding: "15%",
      speed: 500,
      dots:true,
    });

  });

  